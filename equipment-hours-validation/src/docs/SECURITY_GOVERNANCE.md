# Security & Governance

- Store secrets in a vault; never in git
- Least-privilege OAuth scopes per connector
- Role-based dashboard access (exec vs ops vs foremen)
- Bronze raw snapshots retained for auditability
- Environment separation: DEV/TEST/PROD

